<h1> Welcome Bookmark Manager </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL:   <a href='<%=link.url%>'><%= link.url%><a/>
      Tags:  <%= link.tags.map(&:name).join(", ") %>
    </li>
  <% end %>
</ul>
